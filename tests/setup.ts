// Global Vitest setup. Task 1 has no database yet, so there is nothing to
// initialize or truncate here. A later task (DB integration) is expected to
// populate this with per-suite test-database truncation logic, per the note
// in vitest.config.ts: "every suite truncates the same shared test database".
export {};
